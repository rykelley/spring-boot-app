[
  {
    "name": "${container_name}",
    "image": "${image}:${version}",
    "cpu": ${cpu},
    "memory": ${memory},
    ${length(command) > 0 ? "\"command\": ${command}," : ""}
    "essential": true,
    "portMappings": [{
      "containerPort": ${container_http_port},
      "protocol": "tcp"
    }],
    "environment": [
      {"name" : "AWS_REGION", "value" : "${aws_region}"},
      {"name" : "BOOT_DELAY_SEC", "value" : "${boot_delay_seconds}"}
    ]
  }

]
